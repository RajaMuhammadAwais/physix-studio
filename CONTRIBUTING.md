# Contributing

Create focused changes, add or update tests, run `pytest` and `ruff check .`, and document public architectural changes in `docs/adr/`. Physics calculations belong in `src/physix/physics`; visualization adapters must consume state rather than calculate it.
